# requirements.txt
#
# This file specifies the Python package dependencies required for the kgqaHierarchical project.
# It is used by pip (Python's package installer) to install all the necessary libraries
# in a single command:
#
#     pip install -r requirements.txt
#
# Dependencies listed below include frameworks for deep learning (PyTorch), Natural Language
# Processing (Transformers), graph manipulation (NetworkX), and general data science/machine learning.

torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0
networkx>=3.0
tqdm
numpy
scikit-learn

